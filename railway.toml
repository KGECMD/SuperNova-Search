[railway]
name = "supernova-search"
description = "SuperNova Search - Privacy-first search engine by UCXP Project"

[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
